## Project Rules
- Use avoid class-name flags in configs, use simple ones and resolve them into class names when configs are consumed.
- Prefer writing more code and creating new classes if it improves abstractions, over falling back onto defined ones and turning abstractions incorrect/vague.
